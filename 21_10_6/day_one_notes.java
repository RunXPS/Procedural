class day_one{
    public static void main(string args){
    //types
        //Int types
        /*
        byte ==> 1 byte "signed interger"; stores numbers up to 128
        short ==> 2 byte "signed interger"; stores numbers up to 32768
        int ==> 4 byte "signed interger"; stores numbers up to 2147483648
        long ==> 8 byte "signed interger"; stores numbers up to 9,223,372,036,854,775,807

        float ==> four-byte floating point number; 
        double ==> eight-byte floating point number
        boolean ==> true/false
        char ==> unicode charater
        */
        
        //String types
        /*
        -- Do not treat strings like you do in python -- 
        s = "snake"
        s[0] ==> ERROR
        s.charAt(0) ==> 's'

        s.length() ==> 5
        s += "s"
        s ==> 'snakes'

        x.toString ==> sets object to a string
        -- ALL JAVA OBJECTS CAN BE REPRESENTED AS A STRING -- 
        */
    //Binary operators
    /*
        + ==> adds or concatonates
        - ==> subtracts
        % ==> mods
        / ==> divides based on what BOTH types are (doesn't have to be int or double)
    */

    //Math objects
    /*
        Math.pow(2,10) ==> 2^10
        Math.sin()
        Math.cos()
        Math.tan()
    */
    //Array objects
    /*
        add == append
        arrays are homogeneous ==> no more than one type each
    */
    }
}